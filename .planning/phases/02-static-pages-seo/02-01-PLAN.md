---
phase: 02-static-pages-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "Tailwind CSS classes render correctly"
    - "Root layout has base metadata configured"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with metadata base"
      contains: "metadataBase"
    - path: "src/app/globals.css"
      provides: "Tailwind CSS imports"
      contains: "@import"
    - path: "next.config.ts"
      provides: "Next.js configuration"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Bootstrap Next.js 15 App Router with Tailwind CSS v4 and configure root layout with base metadata.

Purpose: Establish the Next.js foundation required for static page generation and SEO metadata. This enables all subsequent plans to build on a working framework.

Output: Working Next.js dev server with Tailwind CSS styling and root layout metadata.
</objective>

<execution_context>
@C:/Users/alexa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alexa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-pages-seo/02-RESEARCH.md

# Phase 1 artifacts (existing data layer)
@src/lib/equipment.ts
@src/data/equipment.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Next.js and Tailwind dependencies</name>
  <files>package.json</files>
  <action>
Install Next.js 15, React 19, and Tailwind CSS v4:

```bash
npm install next@latest react@latest react-dom@latest
npm install -D tailwindcss@latest @tailwindcss/postcss postcss @types/react @types/react-dom
```

Update package.json scripts:
- Add "dev": "next dev"
- Add "build": "next build"
- Add "start": "next start"
- Change "type" from "commonjs" to "module" (required for Next.js App Router)

IMPORTANT: Update tsconfig.json to use Next.js patterns:
- Set "module": "esnext"
- Set "moduleResolution": "bundler"
- Add "paths": { "@/*": ["./src/*"] }
- Add "jsx": "preserve"
- Add "plugins": [{ "name": "next" }]
- Add "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]
  </action>
  <verify>
Run `npm run dev` - server should start on localhost:3000 (will show 404 initially, that's expected)
  </verify>
  <done>Next.js dev server starts without build errors. Package.json has dev/build/start scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 and create root layout</name>
  <files>postcss.config.mjs, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, next.config.ts</files>
  <action>
Create postcss.config.mjs for Tailwind v4:
```javascript
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
```

Create src/app/globals.css with Tailwind v4 imports:
```css
@import "tailwindcss";
```

Create src/app/layout.tsx with:
- Root HTML structure with lang="en"
- Import globals.css
- Metadata export with:
  - metadataBase: new URL('https://laserhairremovalmap.com')
  - title.template: '%s | Laser Hair Removal Map'
  - title.default: 'Is It A Real Laser? | Laser Hair Removal Map'
  - description: 'Verify if your clinic uses a real laser or IPL device. Look up any laser hair removal machine and get instant classification.'
  - openGraph.type: 'website'
  - openGraph.siteName: 'Laser Hair Removal Map'
- Body with children prop, basic Tailwind classes (antialiased, min-h-screen)

Create src/app/page.tsx as placeholder homepage:
- Simple heading "Laser Hair Removal Equipment Checker"
- Paragraph explaining the tool purpose
- Use Tailwind classes for mobile-first responsive styling

Create next.config.ts:
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Enable static export for production
  output: 'export',
};

export default nextConfig;
```

Note: Use output: 'export' for fully static site generation (no server required).
  </action>
  <verify>
Run `npm run dev` and visit http://localhost:3000:
- Page should display without errors
- Tailwind classes should apply (check font rendering, spacing)
- View page source should show metadata in head
  </verify>
  <done>Homepage renders with Tailwind styling. View source shows title and description meta tags. No console errors.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts successfully
2. http://localhost:3000 shows placeholder homepage
3. Tailwind classes render (visible in styling)
4. Page source contains metadataBase-derived meta tags
5. `npm run build` completes without errors
</verification>

<success_criteria>
- Next.js 15 App Router functional
- Tailwind CSS v4 classes apply correctly
- Root layout has metadata configuration
- Homepage renders as placeholder
- Build succeeds for static export
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-pages-seo/02-01-SUMMARY.md`
</output>
