---
phase: 02-static-pages-seo
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/ClassificationBadge.tsx
  - src/components/BrandTierBadge.tsx
  - src/components/Disclaimer.tsx
  - src/components/EquipmentDetails.tsx
  - src/components/TechnologyTermDetails.tsx
  - src/components/JsonLd.tsx
autonomous: true

must_haves:
  truths:
    - "ClassificationBadge shows 'Real Laser' for laser type, 'Not a Laser' for non-laser"
    - "BrandTierBadge displays tier with appropriate styling"
    - "Disclaimer renders permanent warning text"
    - "EquipmentDetails shows all machine information"
    - "TechnologyTermDetails shows technology clarification"
  artifacts:
    - path: "src/components/ClassificationBadge.tsx"
      provides: "Real Laser / Not a Laser badge"
      exports: ["ClassificationBadge"]
    - path: "src/components/BrandTierBadge.tsx"
      provides: "Brand tier display"
      exports: ["BrandTierBadge"]
    - path: "src/components/Disclaimer.tsx"
      provides: "Permanent disclaimer"
      exports: ["Disclaimer"]
    - path: "src/components/EquipmentDetails.tsx"
      provides: "Full machine details layout"
      exports: ["EquipmentDetails"]
    - path: "src/components/TechnologyTermDetails.tsx"
      provides: "Technology term explanation layout"
      exports: ["TechnologyTermDetails"]
    - path: "src/components/JsonLd.tsx"
      provides: "JSON-LD script injection"
      exports: ["JsonLd"]
  key_links:
    - from: "src/components/EquipmentDetails.tsx"
      to: "src/lib/equipment.ts"
      via: "type import"
      pattern: "import.*MachineEntry.*from.*equipment"
    - from: "src/components/ClassificationBadge.tsx"
      to: "src/components/EquipmentDetails.tsx"
      via: "component composition"
      pattern: "ClassificationBadge"
---

<objective>
Create all UI components needed for equipment detail pages: classification badges, brand tier display, disclaimer, and full equipment/technology term layouts.

Purpose: These components render the classification information that is the core value proposition. They must be clear, mobile-responsive, and accessible.

Output: Complete set of reusable components ready for the dynamic [slug] route page.
</objective>

<execution_context>
@C:/Users/alexa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/alexa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-pages-seo/02-RESEARCH.md

# Phase 1 types (for component props)
@src/lib/equipment.ts

# Phase 2 foundation (from 02-01)
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create classification and brand tier badge components</name>
  <files>src/components/ClassificationBadge.tsx, src/components/BrandTierBadge.tsx</files>
  <action>
Create src/components/ClassificationBadge.tsx:
- Accept props: { isRealLaser: boolean | null, technologyType?: string }
- For machines: derive isRealLaser from technologyType === 'laser'
- Three states with Tailwind styling:
  - true (Real Laser): green badge (bg-green-100 text-green-800, border-green-200)
  - false (Not a Laser): red badge (bg-red-100 text-red-800, border-red-200)
  - null (Ask Your Clinic): yellow badge (bg-yellow-100 text-yellow-800, border-yellow-200)
- Use semantic HTML: span with role="status"
- Mobile-first: base size comfortable on mobile, md: prefix for larger on desktop
- Icon consideration: checkmark for real laser, X for not laser, ? for ambiguous

Create src/components/BrandTierBadge.tsx:
- Accept props: { tier: BrandTier }
- Import BrandTier type from @/lib/equipment
- Display human-readable tier names:
  - 'premium-clinical' -> "Gold Standard" (text-amber-700, bg-amber-50)
  - 'standard-clinical' -> "Established" (text-blue-700, bg-blue-50)
  - 'consumer' -> "Home Device" (text-gray-700, bg-gray-50)
  - 'unknown' -> "Unknown Brand" (text-gray-500, bg-gray-100)
- Smaller badge style than classification (it's secondary info)

Both components:
- Export as named exports
- Use 'use client' directive NOT needed (these are Server Components by default - no interactivity)
- Mobile-responsive with Tailwind (base styles = mobile, md: = tablet+)
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
Components export correctly (import test in a scratch file or check with IDE)
  </verify>
  <done>ClassificationBadge shows three states with correct colors. BrandTierBadge shows tier with human-readable labels. Both use semantic HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Create disclaimer and JSON-LD components</name>
  <files>src/components/Disclaimer.tsx, src/components/JsonLd.tsx</files>
  <action>
Create src/components/Disclaimer.tsx:
- No props needed (static content)
- Render as aside element with role="complementary" and aria-label="Medical disclaimer"
- Content (match RESEARCH.md pattern):
  ```
  Important Disclaimer

  This tool provides general information about laser hair removal equipment
  for educational purposes only. It is not medical advice. Individual results
  vary based on skin type, hair color, and other factors. Always consult with
  a qualified practitioner before undergoing any cosmetic procedure.
  ```
- Styling: muted background (bg-gray-50), border, rounded corners, small text
- Responsive: full width on mobile, max-w-2xl centered on larger screens

Create src/components/JsonLd.tsx:
- Accept props: { data: Record<string, unknown> }
- Render script tag with type="application/ld+json"
- Use dangerouslySetInnerHTML with JSON.stringify(data)
- This is intentionally a simple passthrough component
- No 'use client' needed (Server Component)
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
Disclaimer has proper accessibility attributes (check aria-label, role)
  </verify>
  <done>Disclaimer shows medical disclaimer with proper accessibility markup. JsonLd renders valid script tag.</done>
</task>

<task type="auto">
  <name>Task 3: Create equipment and technology term detail components</name>
  <files>src/components/EquipmentDetails.tsx, src/components/TechnologyTermDetails.tsx</files>
  <action>
Create src/components/EquipmentDetails.tsx:
- Accept props: { equipment: MachineEntry }
- Import MachineEntry, BrandTier from @/lib/equipment
- Layout structure (mobile-first):
  ```
  [Classification Badge - prominent, centered on mobile]
  [Machine Name - h1, large]
  [Manufacturer - subtitle]

  [Info Grid - single column mobile, 2 cols on md:]
    - Brand Tier: [BrandTierBadge]
    - Purpose: "Purpose-built for hair removal" or "Multi-purpose platform"
    - Technology: {technologyType} laser/IPL/etc
    - Wavelengths: {wavelengths joined}
    - Skin Types: {skinTypes.displayText}
    - Cooling: {coolingMethod or "Not specified"}

  [Notes section if present]

  [Disclaimer - at bottom]
  ```
- Use Tailwind for responsive grid: grid-cols-1 md:grid-cols-2
- Semantic HTML: article wrapper, h1 for name, dl for info list

Create src/components/TechnologyTermDetails.tsx:
- Accept props: { term: TechnologyTermEntry }
- Import TechnologyTermEntry from @/lib/equipment
- Different layout from machines:
  ```
  [Classification Badge - based on isRealLaser]
  [Term Name - h1]
  [What It Is - prominent paragraph]
  [Why It Matters - explanatory section]
  [Ask Your Clinic - if present, highlight box]
  [Disclaimer]
  ```
- Technology terms need more explanatory text, less specs
- Use isRealLaser directly (can be null for ambiguous terms like SHR)

Both components:
- Import and use ClassificationBadge, BrandTierBadge (for machines), Disclaimer
- Mobile-first responsive design
- Semantic HTML structure
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
Both components import types correctly from @/lib/equipment
  </verify>
  <done>EquipmentDetails renders full machine info with all classification layers. TechnologyTermDetails renders term explanation with clarification content. Both include Disclaimer.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (all components type-check)
2. All components export named exports
3. Components use proper types from @/lib/equipment
4. Tailwind classes follow mobile-first pattern (base = mobile, md: = larger)
5. Accessibility: semantic HTML, ARIA labels where needed
</verification>

<success_criteria>
- ClassificationBadge handles all three states (true/false/null)
- BrandTierBadge shows human-readable tier names
- Disclaimer has proper medical disclaimer content
- EquipmentDetails shows full machine classification
- TechnologyTermDetails shows term clarification
- All components mobile-responsive
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-pages-seo/02-02-SUMMARY.md`
</output>
